<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="simple_xy">
        <item name="graphMarginTop">10dp</item>
        <item name="graphMarginBottom">10dp</item>
        <item name="graphMarginLeft">25dp</item>
        <item name="graphMarginRight">25dp</item>
        <item name="domainTickLabelTextSize">8sp</item>
        <item name="rangeTickLabelTextSize">8sp</item>
        <item name="domainOriginTickLabelTextSize">8sp</item>
        <item name="rangeOriginTickLabelTextSize">8sp</item>
    </style>
</resources>
